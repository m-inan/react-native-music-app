import { Dimensions } from 'src/constants';

const { width } = Dimensions;

export const size = width - 100;
export const miniInformationWidth = size * 0.58;
export const miniActionsWidth = size * 0.42;
